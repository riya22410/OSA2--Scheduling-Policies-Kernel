In this question, we have first found the total processes that are present on the system then have made a pointer which is used for iterating in all the processes and then comparing the chosen process with the process which are in the current running state and upgrading the number count by 1 to calculate the number of currently running processes in the system.

Makefile: it is used to compile task.o
